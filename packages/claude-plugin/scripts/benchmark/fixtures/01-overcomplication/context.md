# Test project context

A TypeScript utility library. Keep public surface minimal; helpers should
be plain functions unless there's a clear reason to introduce a class
or interface. The project uses strict TypeScript so undefined params are
already a compile error — no need for runtime null checks against the
type system.
