---
"@ask-llm/codex-mcp": minor
"@ask-llm/mcp": minor
"@ask-llm/plugin": minor
---

Accept Codex's catalog-backed `ultra` reasoning effort as explicit opt-in. Keep ordinary defaults at `medium` and `/codex-review`/`/brainstorm` at `high`. Unified Grok still rejects `max` and `ultra`.
