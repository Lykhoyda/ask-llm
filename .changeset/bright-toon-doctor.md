---
"@ask-llm/mcp": minor
---

Add an explicit, bounded `doctor --format toon` v1 pilot with structured errors, omission and truncation disclosure, contextual help, and a `--full` escape hatch while preserving default text, `--json`, MCP, and machine contracts.
