---
"@ask-llm/plugin": patch
---

Make the default-on codex-pair broker safe to tear down and recover, keep its socket usable on deep project paths, and give broker reviews the same quota fallback and transient retry as direct reviews.
