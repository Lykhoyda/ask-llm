---
"@ask-llm/mcp": minor
"@ask-llm/plugin": minor
---

Add Codex `preferred` and `sandbox` to unified `ask-llm` (fail closed on other providers) and let plugin Codex workflows use fully pinned unified MCP when no `ask-codex` leaf is present, without stripping provider options (#266).
